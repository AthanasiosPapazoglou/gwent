import 'package:flutter/material.dart';



class Rules extends StatelessWidget {

  static const routeName = '/rules-screen';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('RULES SCREEN', style: TextStyle(color: Colors.red),),
    );
  }
}